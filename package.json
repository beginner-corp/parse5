{
  "name": "@begin/parse5",
  "version": "0.0.4",
  "description": "A bundled version of Parse5 for Lambda Node",
  "type": "module",
  "main": "parse5.mjs",
  "scripts": {
    "build": "esbuild index.mjs --platform=node --format=esm --bundle --outfile=parse5.mjs",
    "test": "tape-es ./test.mjs | tap-arc"
  },
  "devDependencies": {
    "esbuild": "^0.14.39",
    "tap-arc": "^0.3.4",
    "tape-es": "^1.2.17"
  },
  "dependencies": {
    "parse5": "^7.0.0"
  }
}
